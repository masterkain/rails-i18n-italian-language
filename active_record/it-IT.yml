# Author::    Claudio Poli (mailto:claudio@icoretech.org)

# Italian ActiveRecord language file.

it-IT:
  activerecord:
    errors:
      # The values :model :attribute and :value are always available for interpolation
      # The value :count is available when applicable. Can be used for pluralization.
      messages:
        inclusion: "non &egrave; incluso nella lista"
        exclusion: "&egrave; riservato"
        invalid: "&egrave; invalido"
        confirmation: "non coincide con la conferma"
        accepted: "deve essere accettata"
        empty: "non pu&ograve; essere vuoto"
        blank: "non pu&ograve; essere vuoto"
        too_long: "&egrave; troppo lungo (il massimo &egrave; {{count}} lettere)"
        too_short: "&egrave; troppo corto (il minimo &egrave; {{count}} lettere)"
        wrong_length: "&egrave; della lunghezza sbagliata (deve essere di {{count}} lettere)"
        taken: "&egrave; gi&agrave; in uso"
        not_a_number: "non &egrave; un numero"
        greater_than: "deve essere superiore a {{count}}"
        greater_than_or_equal_to: "deve essere superiore o uguale a {{count}}"
        equal_to: "deve essere uguale a {{count}}"
        less_than: "deve essere meno di {{count}}"
        less_than_or_equal_to: "deve essere meno o uguale a {{count}}"
        odd: "deve essere dispari"
        even: "deve essere pari"
        # Append your own errors here or at the model/attributes scope.

      models:
        # Overrides default messages

      attributes:
        # Overrides model and default messages.